# ===========================================
# VARIABLES DE ENTORNO - CONFIGURACION
# ===========================================

# ----- BASE DE DATOS (PostgreSQL) -----
# Opciones gratuitas: Vercel Postgres, Supabase, Neon
#
# Para Vercel Postgres (recomendado):
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
DIRECT_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Para Neon:
# DATABASE_URL="postgresql://user:password@ep-xxx.region.neon.tech/database?sslmode=require"
# DIRECT_URL="postgresql://user:password@ep-xxx.region.neon.tech/database?sslmode=require"

# ----- NEXTAUTH -----
# URL de la aplicacion
# Desarrollo:
NEXTAUTH_URL="http://localhost:3000"
# Produccion (cambiar por tu dominio):
# NEXTAUTH_URL="https://tu-app.vercel.app"

# Secret para firmar tokens JWT - CRITICO EN PRODUCCION
# Generar con: openssl rand -base64 32
# NUNCA uses el valor por defecto en produccion
NEXTAUTH_SECRET="change-this-to-a-random-secret-in-production"

# ----- GOOGLE OAUTH (OPCIONAL) -----
# Si quieres habilitar login con Google:
# GOOGLE_CLIENT_ID="tu-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="tu-client-secret"

# ----- GOOGLE GEMINI (REQUERIDO PARA VOZ) -----
# API key para el servicio de voz con Gemini Live
# Obtener en: https://aistudio.google.com/apikey
GOOGLE_GEMINI_API_KEY="tu-api-key-de-gemini"

# ===========================================
# CHECKLIST PARA VERCEL
# ===========================================
# [ ] Generar NEXTAUTH_SECRET seguro (openssl rand -base64 32)
# [ ] Configurar DATABASE_URL con PostgreSQL
# [ ] Configurar NEXTAUTH_URL con tu dominio HTTPS
# [ ] Agregar variables en Vercel Dashboard > Settings > Environment Variables
